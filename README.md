# تمرین نهایی لیگ علم داده: پیش‌بینی نمرات دانش‌آموزان

به ریپازیتوری لیگ علم داده خوش آمدید. این دوره توسط **انجمن هوش مصنوعی دانشگاه خوارزمی** برگزار می‌شود.
این مخزن شامل منابع آموزشی خودآموز و همچنین پایپ‌لاین استاندارد تمرین نهایی (Modular Pipeline) برای تحلیل و پیش‌بینی نمرات است.

---

## 📚 ساختار فاز اول و منابع آموزشی

فاز اول این دوره شامل دو بخش "یادگیری مفاهیم پایه" و "انجام تمرین نهایی" است. تمامی فایل‌های مورد نیاز شما در پوشه `resources` قرار گرفته‌اند:

- **بخش سوالات و چالش‌ها:** در مسیر `resources/questions/` قرار دارد.
- **بخش پاسخنامه‌ها و کدهای نمونه:** در مسیر `resources/solutions/` قرار دارد.

> **توجه مهم:** برای دریافت اطلاعات دقیق در مورد نقشه راه، فاز اول و فاز دوم، لطفاً فایل PDF هفته اول (موجود در پوشه `resources/questions`) را مطالعه نمایید.

---

## 📂 ساختار فنی پروژه (تمرین نهایی)

کدها به صورت ماژولار طراحی شده‌اند تا توسعه و نگهداری آن‌ها استاندارد باشد:

- فایل `src/data_loader.py`: ماژول بارگذاری داده‌ها.
- فایل `src/preprocessing.py`: ماژول پاکسازی داده‌ها (مدیریت داده‌های پرت و گمشده).
- فایل `src/features.py`: ماژول مهندسی ویژگی (Feature Engineering).
- فایل `src/visualization.py`: توابع رسم نمودارهای تحلیلی.
- فایل `src/models.py`: **(محل کدنویسی شما در تمرین نهایی)** محل تعریف کلاس‌ها و توابع مدل‌سازی.
- فایل `main.py`: اسکریپت اجرایی اصلی که مراحل پایپ‌لاین را مدیریت می‌کند.

---

##  راهنمای مشارکت (GitHub Flow)

برای انجام تمرین نهایی و ارسال کدها، لطفاً مراحل استاندارد زیر را دنبال کنید:

1.  **مرحله Fork:** ابتدا باید این مخزن را فورک (Fork) کنید تا نسخه‌ای از آن در پروفایل شما ایجاد شود.
2.  **مرحله Clone:** سپس مخزن را روی سیستم خود کلون کنید:
    ```bash
    git clone [https://github.com/نام-کاربری-شما/Data-Science-League-Project.git](https://github.com/نام-کاربری-شما/Data-Science-League-Project.git)
    ```
3.  **مرحله Develop:** تغییرات مورد نظر خود را در کدها اعمال کنید:
    - می‌توانید مدل‌های جدید (مانند RandomForest، XGBoost و...) را در فایل `src/models.py` پیاده‌سازی کنید.
    - اگر روش خلاقانه‌ای برای پیش‌پردازش دارید، فایل `src/preprocessing.py` را ویرایش کنید.
    - اگر ویژگی‌های جدیدی کشف کرده‌اید، فایل `src/features.py` را توسعه دهید.
4.  **مرحله Push:** پس از نهایی شدن کدها، تغییرات را به مخزن خود ارسال (Push) کنید.
5.  **مرحله Pull Request:** در نهایت، درخواست ادغام (Pull Request) خود را به مخزن اصلی ارسال کنید.

### معیارهای پذیرش Pull Request
ما از مشارکت‌های شما استقبال می‌کنیم. موارد زیر اولویت پذیرش دارند:
- ارائه مدلی با RMSE کمتر نسبت به مدل پایه.
- پیاده‌سازی ویژگی‌های جدید و خلاقانه در بخش مهندسی ویژگی.
- بهبود روش‌های پاکسازی داده و مدیریت داده‌های پرت.

---

**موفق باشید.**
*انجمن هوش مصنوعی دانشگاه خوارزمی*
