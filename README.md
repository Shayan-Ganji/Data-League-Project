# تمرین نهایی لیگ علم داده: پیش‌بینی نمرات دانش‌آموزان

به ریپازیتوری پروژه نهایی لیگ علم داده خوش آمدید. این رویداد توسط **انجمن هوش مصنوعی دانشگاه خوارزمی** طراحی و برگزار می‌شود.
این مخزن شامل منابع آموزشی فاز اول و همچنین پایپ‌لاین استاندارد پروژه نهایی (Modular Pipeline) برای تحلیل و پیش‌بینی نمرات است.

---

## 📚 ساختار دوره و منابع آموزشی

این دوره شامل دو فاز اصلی است:

### ۱. فاز اول: یادگیری خودآموز
این بخش شامل یادگیری مفاهیم پایه علم داده، پاکسازی داده‌ها و تحلیل اکتشافی است. تمامی فایل‌های مورد نیاز برای این فاز در پوشه `resources` قرار گرفته‌اند:
- **صورت سوالات و چالش‌ها:** در مسیر `resources/questions/`
- **پاسخنامه‌ها و کدهای نمونه:** در مسیر `resources/solutions/`

> **توجه مهم:** برای دریافت اطلاعات دقیق در مورد ساختار فاز اول و دوم و نحوه پیشبرد دوره، لطفاً فایل PDF هفته اول (موجود در پوشه `resources/questions`) را مطالعه نمایید.

### ۲.  تمرین نهایی فاز اول:  مشارکت شما
در این مرحله، شما باید با استفاده از کدهای پایه موجود در این مخزن، مدل‌های یادگیری ماشین خود را توسعه دهید و در تکمیل پروژه مشارکت کنید.

---

## 📂 ساختار فنی پروژه
کدها به صورت ماژولار طراحی شده‌اند تا توسعه و نگهداری آن‌ها استاندارد باشد:

- `src/data_loader.py`: ماژول بارگذاری داده‌ها.
- `src/preprocessing.py`: ماژول پاکسازی داده‌ها (مدیریت داده‌های پرت و گمشده).
- `src/features.py`: ماژول مهندسی ویژگی (Feature Engineering).
- `src/visualization.py`: توابع رسم نمودارهای تحلیلی.
- `src/models.py`: **(محل کدنویسی شما)** محل تعریف کلاس‌ها و توابع مدل‌سازی.
- `main.py`: اسکریپت اجرایی اصلی که مراحل پایپ‌لاین را مدیریت می‌کند.

---

##  راهنمای مشارکت (Contribution)

برای ارسال کدها، مدل‌های پیشنهادی و بهبود پروژه، لطفاً مراحل استاندارد GitHub Flow را دنبال کنید:

1.  **Fork:** ابتدا این مخزن را 'Fork' کنید تا نسخه‌ای از آن در پروفایل شما ایجاد شود.
2.  **Clone:** مخزن را روی سیستم خود کلون کنید:
    ```bash
    git clone [https://github.com/نام-کاربری-شما/Data-Science-League-Project.git](https://github.com/نام-کاربری-شما/Data-Science-League-Project.git)
    ```
3.  **Develop:** تغییرات مورد نظر خود را اعمال کنید.
    - می‌توانید مدل‌های جدید (مانند RandomForest، XGBoost و...) را در `src/models.py` پیاده‌سازی کنید.
    - اگر روش خلاقانه‌ای برای پیش‌پردازش یا ساخت ویژگی جدید دارید، کدهای مربوطه را ویرایش کنید.
4.  **Push:** تغییرات را به مخزن خود ارسال کنید.
5.  **Pull Request:** درخواست ادغام (Pull Request) خود را به مخزن اصلی ارسال کنید.

### معیارهای پذیرش Pull Request
ما از مشارکت‌های شما استقبال می‌کنیم. موارد زیر اولویت دارند:
- ارائه مدلی با RMSE کمتر نسبت به مدل پایه
- پیاده‌سازی ویژگی‌های جدید و خلاقانه در `src/features.py`.
- بهبود روش‌های پاکسازی داده در `src/preprocessing.py`.

---

**موفق باشید.**
*انجمن هوش مصنوعی دانشگاه خوارزمی*

